{% extends 'base.html.twig' %}

{% block body %}
<div class="container">

    <h2>Modifier le style</h2>

    {{ form_start(form) }}
        {{ form_rest(form) }}
     
       <a href="{{ path('admin.song.index') }}" class="btn btn-danger">Annuler</a>

    {{ form_end(form) }}

</div>
   
{% endblock %}